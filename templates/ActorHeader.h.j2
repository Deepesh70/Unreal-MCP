// {{ class_name }}.h — Auto-generated by Unreal-MCP
// Generated: {{ timestamp }}

#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "{{ class_name }}.generated.h"

UCLASS(Blueprintable, Placeable, meta = (DisplayName = "{{ display_name }}"))
class {{ project_api }} {{ class_name }} : public AActor
{
	GENERATED_BODY()

public:
	{{ class_name }}();

protected:
	virtual void BeginPlay() override;

public:
	virtual void Tick(float DeltaTime) override;
{% for prop in properties %}

	UPROPERTY({{ prop.specifiers }})
	{{ prop.type }} {{ prop.name }}{% if prop.default %} = {{ prop.default }}{% endif %};
{% endfor %}
{% for func in functions %}

	UFUNCTION({{ func.specifiers }})
	{{ func.return_type }} {{ func.name }}({{ func.params }});
{% endfor %}
};
