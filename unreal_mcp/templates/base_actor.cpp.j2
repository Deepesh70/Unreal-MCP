// {{ description }}
// Auto-generated by Unreal MCP - do not edit boilerplate manually.

#include "{{ class_name }}.h"

{% if parent_class.startswith("A") -%}
A{{ class_name }}::A{{ class_name }}()
{% else -%}
U{{ class_name }}::U{{ class_name }}()
{% endif -%}
{
	{% if tick_enabled -%}
	{% if parent_class.startswith("A") -%}
	PrimaryActorTick.bCanEverTick = true;
	{% else -%}
	PrimaryComponentTick.bCanEverTick = true;
	{% endif -%}
	{% else -%}
	{% if parent_class.startswith("A") -%}
	PrimaryActorTick.bCanEverTick = false;
	{% else -%}
	PrimaryComponentTick.bCanEverTick = false;
	{% endif -%}
	{% endif -%}
	{% if constructor_body -%}
	{{ constructor_body }}
	{% endif %}
}

{% if parent_class.startswith("A") -%}
void A{{ class_name }}::BeginPlay()
{% else -%}
void U{{ class_name }}::BeginPlay()
{% endif -%}
{
	Super::BeginPlay();
	{% if begin_play_body -%}
	{{ begin_play_body }}
	{% endif %}
}

{% if tick_enabled -%}
{% if parent_class.startswith("A") -%}
void A{{ class_name }}::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);
{% else -%}
void U{{ class_name }}::TickComponent(float DeltaTime, ELevelTick TickType, FActorComponentTickFunction* ThisTickFunction)
{
	Super::TickComponent(DeltaTime, TickType, ThisTickFunction);
{% endif %}
	{% if tick_body -%}
	{{ tick_body }}
	{% endif %}
}

{% endif -%}

// --- Function Implementations ---
{% for func in functions %}
{% if parent_class.startswith("A") -%}
{{ func.cpp_return_type }} A{{ class_name }}::{{ func.name }}({{ func.param_string }})
{% else -%}
{{ func.cpp_return_type }} U{{ class_name }}::{{ func.name }}({{ func.param_string }})
{% endif -%}
{
	{% if func.body -%}
	{{ func.body }}
	{% endif %}
}

{% endfor %}
