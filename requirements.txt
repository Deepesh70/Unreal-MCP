# ── Core ──────────────────────────────────────────────────────────────
fastmcp
websockets
jinja2
pydantic

# ── LangChain Core ────────────────────────────────────────────────────
langchain
langchain_core
langchain_mcp_adapters

# ── LLM Providers ────────────────────────────────────────────────────
langchain_groq               # Groq Cloud (Llama 3.3 70B)
langchain_ollama             # Local Ollama (70B+ models)
langchain_google_genai       # Google Gemini (2.5 Pro / Flash)

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv
legacy-cgi                   # Python 3.13+ compat

# ── Optional RAG Embeddings (Only needed if using local offline vector store)
# langchain_chroma
# langchain_huggingface